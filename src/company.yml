# Company-wide values shared by every signature.
# Changing anything here changes every employee's signature on the next build.

name: CyberSkill
tagline: Turn Your Will Into Real

# Published site root. Every asset URL in every signature is built from this,
# so it must be the FINAL public URL before anyone installs a signature -
# Gmail re-hosts images through its own proxy at paste time, and a signature
# pasted against an old URL can only be fixed by pasting again.
#
# custom_domain is written to docs/CNAME on every build. GitHub creates that
# file when you first save the domain in Settings, but generating it here
# means a clean rebuild can never silently detach the domain.
base_url: https://signatures.cyberskill.world/
custom_domain: signatures.cyberskill.world

# Where the site sends people who need a signature or want to change theirs.
# Used to build the request links on the help page - no trailing slash.
repo: https://github.com/cyberskill-official/signatures

# Allow search engines to index the site.
#
# Off by default. Every employee's name, job title, work email and photo are
# on this site, and making that harvestable is a decision somebody should make
# deliberately rather than inherit. Staff reach the site by link, so indexing
# buys little. Set to true if you would rather it be findable by search - the
# build then emits a permissive robots.txt and a sitemap.
#
# Either way the images stay publicly fetchable. Mail clients have to be able
# to load them, so that part cannot be closed off.
index_site: false

# Domains a staff signature address may use. A record with an address outside
# this list fails the build rather than publishing, which catches the mistake
# somebody will eventually make: copying the template and pasting a personal
# address into it.
#
# Exact matches only - a subdomain you actually send from has to be listed.
email_domains:
  - cyberskill.world

# Channels every employee shares. A person can override any of these in their
# own record, or set it to null to drop the row entirely.
website: cyberskill.world
website_href: https://cyberskill.world

# Add a channel the day somebody owns the inbox, not before. A link that
# nobody reads is worse than no link: it looks reachable, and whoever writes
# to it hears nothing back and draws a conclusion about the company.
#
# Zalo goes here once it is set up - the icon set already carries it.
socials:
  - label: LinkedIn
    href: https://linkedin.com/company/cyberskill
